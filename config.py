"""配置文件 — 集中管理所有可调参数"""

# ============================================================
# 球场参数
# ============================================================

# 标准单打球场尺寸 (米)
COURT_LENGTH = 13.4           # 底线到底线 (单打/双打相同)
COURT_WIDTH_SINGLES = 5.18    # 单打边线宽度
COURT_WIDTH_DOUBLES = 6.10    # 双打边线宽度
COURT_WIDTH = COURT_WIDTH_SINGLES  # 向后兼容别名 (默认单打)

# 场地判定: 宽/长比阈值
ASPECT_RATIO_SINGLES = 5.18 / 13.4   # ≈ 0.3866
ASPECT_RATIO_DOUBLES = 6.10 / 13.4   # ≈ 0.4552
# 判定阈值: 如果透视校正后的宽长比 > 0.42 → 双打场地, 否则 → 单打场地
ASPECT_RATIO_THRESHOLD = (ASPECT_RATIO_SINGLES + ASPECT_RATIO_DOUBLES) / 2  # ≈ 0.4209

# 发球区定义 (距网距离, 米)
SHORT_SERVICE_LINE = 2.0       # 前发球线 (单打/双打相同)
LONG_SERVICE_LINE_DOUBLES = 11.4  # 双打后发球线
# 单打后发球线 = 底线 = 13.4m
# 双打发球区深度 = 11.4 - 0 = 后发球线到网(简化: 前发球线2.0 到 后发球线11.4)

# 球场俯视图尺寸 (像素) — 保持统一尺寸, 实际米制通过 COURT_LENGTH/WIDTH 换算
BIRDS_EYE_WIDTH = 800
BIRDS_EYE_HEIGHT = 400

# ============================================================
# 球场检测参数
# ============================================================

# Canny 边缘检测
CANNY_LOW = 50
CANNY_HIGH = 150

# HoughLinesP 直线检测
HOUGH_RHO = 1
HOUGH_THETA = 3.14159 / 180
HOUGH_THRESHOLD = 80
HOUGH_MIN_LINE_LENGTH = 100
HOUGH_MAX_LINE_GAP = 50

# 直线聚类: 方向容差 (度)
LINE_ANGLE_TOLERANCE = 10.0
# 平行线最小间距 (像素) — 用于区分不同场地
MIN_LINE_SPACING = 50

# 场地选择: 候选场地最小面积 (相对于图像面积的比例)
MIN_COURT_AREA_RATIO = 0.05

# 不完整场地: 最少需要的边数
MIN_EDGES_REQUIRED = 3

# ============================================================
# 追踪参数
# ============================================================
PLAYER_MIN_CONFIDENCE = 0.5      # 球员检测最低置信度
SMOOTHING_WINDOW = 3             # 坐标平滑窗口 (帧)
TRACK_MAX_AGE = 30               # 追踪丢失最大帧数

# YOLO 模型
POSE_MODEL = "yolov8n-pose.pt"   # 人体关键点检测模型

# COCO 关键点索引
COCO_LEFT_ANKLE = 15
COCO_RIGHT_ANKLE = 16

# ============================================================
# 就绪检测参数
# ============================================================
VELOCITY_THRESHOLD = 0.5         # 就绪状态速度阈值 (m/s)
READY_DURATION = 1.5             # 就绪持续时间 (秒)
COOLDOWN_DURATION = 3.0          # 事件冷却时间 (秒)
ZONE_TOLERANCE = 1.0             # 发球区容差 (米) — 放宽以应对坐标抖动

# ============================================================
# 比分规则参数
# ============================================================
MAX_SCORE = 30                   # 最大单局分数 (21分制)
MIN_SCORE_DIFF = 2               # 平分时最小分差
WIN_SCORE = 21                   # 常规获胜分数

# ============================================================
# 渲染参数
# ============================================================
OVERLAY_OPACITY = 0.6            # 覆盖层透明度
OVERLAY_WIDTH = 250              # 覆盖层宽度 (像素)
OVERLAY_HEIGHT = 500             # 覆盖层高度 (像素)
TRAIL_LENGTH = 20                # 轨迹显示长度 (帧)

# 颜色定义 (BGR 格式, OpenCV 使用 BGR)
COLOR_PLAYER_A = (0, 0, 255)       # 球员A = 红色 (BGR)
COLOR_PLAYER_B = (255, 0, 0)       # 球员B = 蓝色 (BGR)
COLOR_TRAIL_A = (0, 165, 255)      # A 轨迹 = 橙色
COLOR_TRAIL_B = (255, 255, 0)      # B 轨迹 = 青色
COLOR_COURT_BG = (0, 100, 0)       # 球场背景 = 深绿
COLOR_COURT_LINE = (255, 255, 255) # 球场线 = 白色
COLOR_TEXT = (255, 255, 255)       # 文字 = 白色
COLOR_TEXT_BG = (0, 0, 0)          # 文字描边 = 黑色
COLOR_OVERLAY_BG = (50, 50, 50)    # 覆盖层背景 = 深灰
COLOR_ZONE_ACTIVE = (0, 255, 255)  # 活跃发球区 = 黄色

# ============================================================
# 输出参数
# ============================================================
OUTPUT_VIDEO_CODEC = "mp4v"      # 输出视频编码
OUTPUT_FPS = 25                   # 输出帧率 (若不指定则用原视频帧率)

# ============================================================
# 日志参数
# ============================================================
LOG_LEVEL = "INFO"               # 日志级别: DEBUG, INFO, WARNING, ERROR
